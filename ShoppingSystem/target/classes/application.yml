spring:
  application:
    name: ShoppingSystem
  mail:
    host: smtp.qq.com  # QQ 邮箱 SMTP 服务器
    port: 465           # SSL 端口
    username: yu-xingyou@foxmail.com  # 你的 QQ 邮箱
    password: geoujynkvqtvcjde  # 授权码（不是 QQ 密码！）
    properties:
      mail:
        smtp:
          ssl:
            enable: true  # 启用 SSL
          auth: true


  datasource:
    url: jdbc:mysql://localhost:3306/shopping_system
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 0927

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB
      enabled: true

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
server:
  port: 8080

# JWT配置
jwt:
  signKey: eGluZ3lvdQ==
  expire: 43200000

#阿里云OSS
aliyun:
  oss:
    endpoint: https://oss-cn-beijing.aliyuncs.com
    bucketName: shopping-exam
    region: cn-beijing

